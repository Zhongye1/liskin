- AGW（API Gateway）：面向“整个业务系统”的统一入口网关，负责鉴权、路由
  、通用中间件、请求转发。
- Model Gateway（模型网关）：面向“模型调用域”的专用网关，负责多模型适
  配、模型路由、计费与配额、模型调用观测与异常处理。

可以理解成：

- AGW 是“城市大门”（所有业务 API 先过这里）
- Model Gateway 是“模型机房前置调度层”（专门管 LLM 请求）
